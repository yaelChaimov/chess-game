#include <iostream>
#include "Board.h"

int main()
{
    Board board;
    board.printBoard();
   // board->_chessBoard[4][4]->move(5,4, board);
   // std::cout << std::endl;
   // board->printBoard();

}